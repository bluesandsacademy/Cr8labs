# Contact form delivery (Resend). Copy to .env.local; never commit real values.
RESEND_API_KEY=
# Where submissions are delivered.
CONTACT_TO_EMAIL=
# The verified sender on your Resend domain, e.g. "CR8LAB <hello@yourdomain>".
CONTACT_FROM_EMAIL=
